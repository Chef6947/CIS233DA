form {
    display: flex;
    flex-direction: column;
    align-items: center;
  }
  
  label {
    margin-top: 10px;
  }
  
  input[type="text"],
  input[type="date"],
  input[type="checkbox"] {
    margin-bottom: 10px;
  }
  
  button {
    margin-top: 20px;
  }
  